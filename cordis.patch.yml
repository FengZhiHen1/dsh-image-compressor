# dsh-image-compressor bundle patch: mounts the plugin into a profile layer
# stack. Dual-layer plugin (C-02, 2026-08 修订)：the profile's cordis layers
# see exactly this single row — the host apply registers the read_image
# tools/execute wrapper; the client half enters the boot graph through the
# dsh.client manifest.
- insert:
    - id: dsh-image-compressor
      name: 'dsh-image-compressor'
